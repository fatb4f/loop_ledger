# Scope

Focused review bundle for the current Asset Control Model decision that freezes
the loop-pipeline working model.

Included:

- the new ACM working-model documents and the updated policy authority note
- the root ACM README and policy framework README that point at the working model
- the implicated Codex loop and git-flow control assets
- the implicated `loop_ledger` schema and controller scripts that currently
  enforce evidence-bundle materialization

Excluded:

- full implementation trees outside the directly implicated controller surface
- generated bundle directories for unrelated projects
- large runtime artifacts not needed to review this modeling decision
