<!-- generated by skills/implement/scripts/generate_implement_packet.py -->
<!-- objective: Implement the read-only local prompt-registry overlay pipeline: loader, resolver, assembler, and authority verifier for the approved in-scope workflows and profiles -->

# Implement Packet

## artifact_paths
- `wt`: `/home/_404/src/_worktrees/workbench-prompt-registry-001.iteration-0002`
- Packet: `$wt/IMPLEMENT/IMPLEMENT.md`
- Metadata: `$wt/IMPLEMENT/packet.meta.json`
- Response instance: `$wt/IMPLEMENT/response.instance.json`
- Response manifest: `$wt/IMPLEMENT/response.manifest.json`
- Response validation: `$wt/IMPLEMENT/response.validation.json`

## execution_context
- Parent plan issue: `workbench-prompt-registry-operationalization-001`
- Objective: Implement the read-only local prompt-registry overlay pipeline: loader, resolver, assembler, and authority verifier for the approved in-scope workflows and profiles
- Mode: `dry-run`
- Request instance: `PLAN/request.instance.json`
- Contract bindings: `PLAN/contract.bindings.json`

## scope_lock
- Deliverable IDs in scope:
  - `D1` loader emits deterministic registry input model
  - `D2` resolver emits workflow/profile resolution output
  - `D3` assembler emits prompt assembly plan
  - `D4` authority verifier emits authority/parity results
- Out-of-scope items:
  - RAG adapters
  - read-only MCP facade for registry and RAG
  - live Codex session injection
  - prompt optimization experiments
  - control authority renames
- Locked refs/SHAs:
  - asset-control-model head: `25d159c89ca099bca1623988b0f60f6127c60295`
  - dotfiles tracked head: `5ebb0404051985e9a1e25cd0c87ac2783cc92b51`

## issue_map
| Deliverable ID | Parent issue | Sub-issue | Owner | Status |
|---|---|---|---|---|
| D1 | workbench-prompt-registry-operationalization-001 | loader | codex | done |
| D2 | workbench-prompt-registry-operationalization-001 | resolver | codex | done |
| D3 | workbench-prompt-registry-operationalization-001 | assembler | codex | done |
| D4 | workbench-prompt-registry-operationalization-001 | authority-verifier | codex | done |

## work_queue
| Order | Deliverable ID | Action | Validation gate | Rollback |
|---|---|---|---|---|
| 1 | D1 | load manifest, retrieval indexes, profiles, workflows, and snippets into a normalized read-only model | registry schema + manifest membership parity | revert packet/state to NEEDS_INFO |
| 2 | D2 | resolve selected workflow/profile set for `workflow.contract_first` and `workflow.git_flow.integration_worktree` with `default` and `review` profiles | deterministic resolution + unresolved-ref deny | revert packet/state to NEEDS_INFO |
| 3 | D3 | emit assembled prompt plan with selected workflow, profile, snippet order, and source-of-truth refs | artifact shape + stable ordering | revert packet/state to NEEDS_INFO |
| 4 | D4 | emit authority verification and registry parity results | authority refs resolve under `control/` and no legality drift | revert packet/state to NEEDS_INFO |

## evidence_required
| Deliverable ID | Evidence required | Proof ref | Status (pending|met|blocked) |
|---|---|---|---|
| D1 | normalized loader output and manifest-membership proof | `IMPLEMENT/resolved.overlay.json` | met |
| D2 | deterministic workflow/profile resolution output | `IMPLEMENT/resolved.overlay.json` | met |
| D3 | assembled prompt plan with ordered snippets and authority refs | `IMPLEMENT/assembled.prompt.plan.json` | met |
| D4 | authority verification and parity report | `IMPLEMENT/authority.verification.json`, `IMPLEMENT/registry.parity.check.json` | met |

## gates
- Evidence gate status: `PASS`
- Additional gate checks:
  - resolved workflow ids must exist in `prompt-registry/manifest.json`
  - every resolved authority ref must resolve under `control/`
  - overlay outputs may not redefine legality, gating, or promotion semantics
  - unresolved workflow/profile/snippet refs are fatal

## execution
- Commands/payloads executed:
  - `python3 skills/implement/scripts/generate_implement_packet.py ...`
  - `python3 policy_framework/scripts/emit_prompt_overlay_artifacts.py --wt /home/_404/src/_worktrees/workbench-prompt-registry-001.iteration-0002 --contract-root /home/_404/src/dotfiles/config/codex`
- Git flow reference: `control/git-flow/implement/README.md`

## results
- Per-deliverable outcomes:
  - `D1` emitted `IMPLEMENT/resolved.overlay.json` with manifest-backed workflow/profile resolution and `status = PASS`
  - `D2` emitted `IMPLEMENT/resolved.overlay.json` with deterministic workflow/profile selection for the in-scope profiles and workflows
  - `D3` emitted `IMPLEMENT/assembled.prompt.plan.json` with stable snippet ordering and authority refs for `default` and `review`
  - `D4` emitted `IMPLEMENT/authority.verification.json` and `IMPLEMENT/registry.parity.check.json`, both with `status = PASS`
- Regressions observed:
  - none yet

## rollback
- Rollback invoked: `no`
- Rollback details:
  - if any resolved overlay drifts from `control/` authority or parity fails, keep the worktree, mark the loop `NEEDS_INFO`, and do not advance to `VERIFY`

## closeout_decision
- Status: `APPROVED`
- Rationale:
  - runtime artifacts and evidence for `D1-D4` are emitted and passing, and the implement-complete review accepted the slice for transition to `VERIFY`
- Next action:
  - open the `VERIFY` packet and run the verify-phase contract checks
