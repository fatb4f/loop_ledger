# Consolidated bundle

This bundle is the fully consolidated `codex/` tree.

Locked constraints:
- `prompt.txt` unchanged
- git-flow control lives under `control/git-flow/`
- prompt registry shipped under `prompt-registry/`
- active `plan`, `implement`, and `loop` skills are aligned to the bounded-batch git flow
- no active sweep/preflight references remain in `skills/plan`, `skills/implement`, or `skills/loop`

Validation snapshot:
- plan packet generation + validation: pass
- implement packet generation + validation: pass
- loop handoff validation: pass
- contract bridge validation: pass
