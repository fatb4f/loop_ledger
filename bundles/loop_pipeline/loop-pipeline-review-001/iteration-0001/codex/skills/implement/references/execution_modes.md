# Execution Modes

- `dry-run` (default): generate and verify commands/payloads without mutation.
- `apply`: perform mutations only when approval and evidence gates are satisfied.

Never skip dry-run for new or changed workflows.
