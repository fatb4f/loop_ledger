# Evidence Gate

`evidence_required` is mandatory per deliverable.

Gate outcomes:
- `PASS`: all required evidence is present and valid.
- `FAIL`: required evidence missing/invalid.
- `PENDING`: evidence collection incomplete.

`apply` is permitted only when gate is `PASS`.
